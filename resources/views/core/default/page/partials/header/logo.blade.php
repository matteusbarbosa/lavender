<h1>

    <a href="{{ $url }} ">

        <image src="{{ asset(config('store.logo')) }}" alt="{{ config('store.name') }}" width="55" height="55" />

        {{ config('store.name') }}

    </a>

</h1>